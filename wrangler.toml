name = "vekku-bun"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
# DATABASE_URL will be set via `wrangler secret put DATABASE_URL`
# WORKER="true" is set automatically by the runtime env check if we want, or we can set it here.
WORKER = "true"

# If we were using Cloudflare D1 or KV, we'd define them here.
# For Neon, we just use the TCP/HTTP connection.
